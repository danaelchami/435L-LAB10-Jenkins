pipeline {
    agent any

    environment {
        VIRTUAL_ENV = 'venv'
    }

    stages {

       stage('Setup') {
            steps {
                script {
                    bat """
                    if exist ${VIRTUAL_ENV} rmdir /s /q ${VIRTUAL_ENV}
                    python -m venv ${VIRTUAL_ENV}
                    call ${VIRTUAL_ENV}\\Scripts\\activate && python -m pip install --upgrade pip
                    call ${VIRTUAL_ENV}\\Scripts\\activate && pip install -r requirements.txt
                    call ${VIRTUAL_ENV}\\Scripts\\activate && pip list
                    """
                }
            }
        }


        stage('Lint') {
            steps {
                script {
                    bat "call ${VIRTUAL_ENV}\\Scripts\\activate && flake8 app.py"
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    bat "call ${VIRTUAL_ENV}\\Scripts\\activate && pytest"
                }
            }
        }

        stage('Coverage') {
            steps {
                script {
                    bat """
                    call ${VIRTUAL_ENV}\\Scripts\\activate
                    coverage run -m pytest
                    coverage report -m
                    """
                }
            }
        }

        stage('Security Scan') {
            steps {
                script {
                    bat "call ${VIRTUAL_ENV}\\Scripts\\activate && bandit -r ."
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    echo "Deploying application..."
                    bat "call ${VIRTUAL_ENV}\\Scripts\\activate && python app.py"
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
